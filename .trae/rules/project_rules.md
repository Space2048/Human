# HUMAN · AI 模拟人类 — 项目规则

## 项目定位

用 LLM 创建一个"数字人类"：持续存在、持续思考、拥有记忆、能感知环境、能动手做事、可被教育与塑造的拟人个体。

## 目录结构

```
.
├── docs/                      # 研究文档（HTML，浏览器直接打开）
│   ├── index.html             # 文档中心首页
│   ├── assets/style.css       # 全站统一样式（唯一样式来源）
│   ├── 研究/                  # 理论研究文档
│   │   ├── 01-项目概述与目标.html
│   │   ├── 02-智能体核心架构-工具系统.html
│   │   ├── 03-自我思考循环与瞬时记忆.html
│   │   ├── 04-长久记忆系统.html
│   │   ├── 05-教育系统与人格属性.html
│   │   ├── 06-综合架构设计与路线图.html
│   │   ├── 07-反馈系统.html
│   │   ├── 08-模拟人类节奏.html
│   │   ├── 09-思考循环的内部结构.html
│   │   ├── 10-动机系统.html
│   │   ├── 11-时间与心境.html
│   │   ├── 12-表达与深度整理.html
│   │   ├── 13-技术架构设计.html
│   │   ├── 14-核心引擎设计.html
│   │   ├── 15-记忆服务设计.html
│   │   └── 16-工具与执行层设计.html
│   └── 参考项目/              # 参考项目调研
│       └── 01-参考项目调研.html
└── .trae/rules/project_rules.md   # 本文件
```

## 人类抽象模型（设计骨架）

子系统（相互咬合）：

1. **手脚 · 工具**：眼睛（感知）/ 手（获取）/ 脚（行动）
2. **大脑 · 思考**：单轮四步骨架（感知→检索→斟酌→产出），产出念头 / 意图 / 印记
3. **瞬时记忆 · 工作台**：思考结果 + 环境影响 + 人类交互 + 工具反馈
4. **长久记忆**：RAG 库（事实）+ 大纲索引（结构）+ 数据模糊（遗忘）
5. **教育与身份**：人类随时交流 + 初始提示词；年龄 / 样貌 / 性格定位 / 价值观 / 动机配方
6. **动机系统**：欲望源（身份）× 强度（记忆）× 调节（心境）—— 主动性的燃料
7. **反馈系统**：六条上行通道 + 快慢双回路 + 路由器 + 累积器 / 心境
8. **节奏 · 时间**：五条节奏线（昼夜 / 注意力 / 情绪 / 动机 / 记忆）+ 主观时间轴 + 心境模型
9. **表达与整理**：内心独白 ↔ 对外言语；深度整理（反思 / 重排 / 清理 / 微调）

两条主线：
- **主循环**（活着）：感知 → 思考 → 行动 → 反馈
- **慢循环**（成长）：瞬时 → 长久 → 人格 → 行为（深度整理为兑现点）

贯穿两线的是**节奏**（研究 08）：时间轴定骨架、心境染色、注意力节律决定思考形态。

## 文档规则

### 风格规则

- **亮色扁平简约朴实**：白底浅灰背景、细边框、无渐变、无厚重阴影、无发光效果。
- **样式统一**：所有 HTML 只引用 `docs/assets/style.css`，禁止内联样式与页面私有样式。
- **配色**：主色蓝 `#3b6df0`，辅色绿 / 琥珀，仅用于强调与分类徽章。

### 内容规则

- **设计阶段只做设计**：当前阶段停留在概念、结构、设计原则层面，不细化到具体功能模块、接口、表结构、技术实现。
- **每个文档回答"设计上怎么想"**，而不是"代码怎么写"。
- **保持精炼**：每页聚焦一个主题，小节不超过 6 个，避免堆砌。

### 结构规则

- 顶部导航：首页 / 研究文档 / 参考项目，active 页高亮。
- 每页结构：crumbs → 标题 → 副标题 → 徽章 meta → 正文 section → 页脚（含上一篇/下一篇翻页）。
- 页内使用统一组件：`section`（带 h2）、`callout`（提示块）、`grid + card`、`table`、`pre`、`timeline`。

### dynamic-ui 内嵌图规则（例外条款）

- 架构类图优先使用 `dynamic-ui` 技能包绘制（`/workspace/dynamic-ui-src/`，遵循 `SKILL.md` 与 `tokens/visual-tokens.md`）。
- 内嵌到文档时：widget 的 CSS 变量定义在作用域类（如 `.dui-fb`）内，所有类名加 `dui-` 前缀，避免与 `docs/assets/style.css` 冲突；不使用 `:root` 全局定义。
- 图内文字使用中文；标签 2-5 词、连接标签 1-3 词；一个图一个焦点。

### 语言规则

- 全站中文，术语统一（大脑、瞬时记忆、长久记忆、教育系统、身份认知、手脚、工具）。
- 口语化但不失专业，避免翻译腔。
- 不添加任何代码注释；无版权图片，图标使用 emoji。

## 开发阶段约束

- **安全**：自主性每加一分，护栏同步加一分（权限、审计、人类叫停）。
- **可观测**：思考、记忆、行动均可回看，这是可被"教育"的前提。
- **可重置**：教育与记忆支持回滚。

## 阶段路线

能活（核心循环）→ 有手（工具与世界）→ 有记忆（长久记忆）→ 有性格（教育与身份）。

## 技术路线（研究 13-16）

- 技术栈：Python 3.12 + LiteLLM（多模型）+ asyncio 自研引擎 + ChromaDB + SQLite/JSON + Playwright。
- 五层架构：接口 / 引擎 / 状态 / 记忆 / 行动，外加贯穿全局的观测层。
- 三原则：先通后优、可观测优先、层间走协议。
- 引擎：调度器心跳 + 单轮四步（感知→检索→斟酌→产出）+ 任务栈打断恢复。
- 记忆：向量只做检索，记忆感来自大纲索引 + 衰减调度；遗忘不物理删除。
- 工具：统一注册表 + Function Calling；安全五步（授权→校验→确认→隔离→审计）。
